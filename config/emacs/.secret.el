;;; .secret.el --- -*- lexical-binding: t -*-
;;;
;;; Commentary:
;;;
;;; Code:

(setq my/own-real-name "Rafael Mironiuc Marte")
(setq my/own-email     "rxf4e1@tuta.io")
(setq my/own-nick      "rxf4el")
(setq my/own-passwd    "@z9u94NuR$")

(setq vc-user-login-name     my/own-nick)
(setq calendar-latitude      -23.5)
(setq calendar-longitude     -46.6)
(setq calendar-location-name "São Paulo, Sp")
